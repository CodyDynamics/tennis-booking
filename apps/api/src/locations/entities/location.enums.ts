export enum LocationVisibility {
  PUBLIC = "public",
  PRIVATE = "private",
}

export enum MemberCourtPriceBasis {
  DISCOUNT_FROM_PUBLIC = "discount_from_public",
  FIXED_MEMBER_RATE = "fixed_member_rate",
}
